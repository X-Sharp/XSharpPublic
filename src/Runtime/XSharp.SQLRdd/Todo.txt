- when top or bottom of subset fetch next/previous batch of records
- add max buffer size property. When datatable is > buffersize delete old rows
- reccount = select count(*) from table, do not cache this
- index condition does not work
- set order to different order should put on same row, that row is the first in the new datatable
- when order is changed, the datatable should be cleared and refilled with the new order
- properties to save if we have seen top or bottom of the table
- do not use temp file, create rdd without the temp file. Can we inherit from Workarea or DBF?
- Enforce the recno column. Throw an exception when there is no recno column
- 

declare @pageno int
select @pageno = 1
select  *  from orders
ORDER BY orderid
OFFSET (@pageno - 1) * 50 ROWS FETCH NEXT 50 ROWS ONLY;

Goto: just the row, or determine the page number and then fetch the page with the record
the mechanism to determin the page number is also needed to determine OrderKeyNo


Record Locks
- Connection/Login 
