<platformui:DialogWindow x:Class="XSharp.LanguageService.Editors.LightBulb.CtorParamsDlg"
                         x:ClassModifier="internal"
                         x:Name="dialog"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:platformui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                         xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
                         Title="Pick members"
                         Height="450" Width="550"
                         MinHeight="450" MinWidth="550"
                         ResizeMode="NoResize"
                         ShowInTaskbar="False"
                         WindowStartupLocation="CenterOwner"
                         toolkit:Themes.UseVsTheme="True"
                         >
    <Window.Resources>
      <RoutedUICommand x:Key="MoveUp" />
      <RoutedUICommand x:Key="MoveDown" />
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource MoveUp}" Executed="MoveUp_Click" />
        <CommandBinding Command="{StaticResource MoveDown}" Executed="MoveDown_Click" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="Up" Modifiers="Alt" Command="{StaticResource MoveUp}" />
        <KeyBinding Key="Down" Modifiers="Alt" Command="{StaticResource MoveDown}" />
    </Window.InputBindings>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0"
                  Header="Pick members to be used as constructor parameters"
                  Foreground="{DynamicResource {x:Static platformui:ThemedDialogColors.HeaderTextBrushKey}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ListView x:Name="listMembers"
                          Grid.Column="0"
                          Margin="9"
                          SelectionMode="Single"
                          ItemsSource="{Binding MemberItems}"
                          SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                          Background="{DynamicResource {x:Static platformui:ThemedDialogColors.WindowPanelBrushKey}}"
                          Foreground="{DynamicResource {x:Static platformui:ThemedDialogColors.ListBoxTextBrushKey}}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="350">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                      Width="Auto"
                                                      Focusable="False"
                                                      Margin="0,0,8,0"/>
                                            <Image Source="{Binding Icon}"
                                                   Width="16"
                                                   Height="16"
                                                   Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding Name}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Column="1"
                            VerticalAlignment="Top"
                            Margin="0,9,9,0">
                    <Button Name="btnUp"
                            Content="Up"
                            Width="105"
                            Height="30"
                            Margin="0,0,0,6"
                            Command="{StaticResource MoveUp}"/>
                    <Button Name="btnDown"
                            Content="Down"
                            Width="105"
                            Height="30"
                            Margin="0,0,0,30"
                            Command="{StaticResource MoveDown}"/>
                    <Button Name="btnSelect"
                            Content="Select All"
                            Width="105"
                            Height="30"
                            Margin="0,0,0,6"
                            Click="btnSelect_Click"/>
                    <Button Name="btnDeselect"
                            Content="Deselect All"
                            Width="105"
                            Height="30"
                            Click="btnDeselect_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Right"
                    Margin="0,11,0,0"
                    Orientation="Horizontal">
            <Button x:Name="btnOk"
                    Content="OK"
                    Width="75"
                    Height="30"
                    Margin="0,0,6,0"
                    Click="btnOk_Click"
                    IsDefault="True"/>
            <Button x:Name="btnCancel"
                    Content="Cancel"
                    Width="75"
                    Height="30"
                    Click="btnCancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</platformui:DialogWindow>