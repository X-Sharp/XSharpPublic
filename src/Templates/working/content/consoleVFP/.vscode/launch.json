{
"version": "0.2.0",
    "configurations": [
        {
            "name": "X# Launch",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/net8.0/${workspaceFolderBasename}.dll",
            "console": "integratedTerminal",
            "stopAtEntry": false
        }
    ]
}