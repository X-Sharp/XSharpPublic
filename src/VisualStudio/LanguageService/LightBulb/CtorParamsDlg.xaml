<Window x:Class="XSharp.LanguageService.Editors.LightBulb.CtorParamsDlg"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
        xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
        toolkit:Themes.UseVsTheme="True"
        Title="Pick members"
        Width="550" Height="430"
        MinWidth="400" MinHeight="300"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Pick members to be used as constructor parameters">
            <Grid Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="115"/>
                </Grid.ColumnDefinitions>

                <ListView x:Name="_listView"
                          Grid.Column="0"
                          Margin="0,0,8,0"
                          SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Members" Width="340">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                      Margin="0,0,4,0"
                                                      VerticalAlignment="Center"/>
                                            <imaging:CrispImage Width="16" Height="16"
                                                                 Moniker="{Binding Moniker}"
                                                                 Margin="0,0,4,0"
                                                                 VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Name}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                    <Button x:Name="_btnUp" Content="Up" Height="28" Margin="0,0,0,4" Click="OnUp"/>
                    <Button x:Name="_btnDown" Content="Down" Height="28" Margin="0,0,0,16" Click="OnDown"/>
                    <Button x:Name="_btnSelectAll" Content="Select All" Height="28" Margin="0,0,0,4" Click="OnSelectAll"/>
                    <Button x:Name="_btnDeselectAll" Content="Deselect All" Height="28" Click="OnDeselectAll"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <Button Content="OK" Width="80" Height="28" Margin="0,0,8,0" IsDefault="True" Click="OnOk"/>
            <Button Content="Cancel" Width="80" Height="28" IsCancel="True" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
